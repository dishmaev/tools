
## with cloud ci+issues tracker, gitflow repo, distrib repos

#- make distrib repos, if now exist
#- sign in/up on github/bitbucket
#- new empty git repo on github/bitbucket, with issues tracker
#- make gitflow repo with tools
#- install IDE, like NetBeans, integrate it with gitflow repo
#- add source project to repo
#- integrate git repo with cloud CI, like semaphoreci.com
#- set up integrate git+ci with distrib repos

#- git push and check of CI work result, like Docker image
