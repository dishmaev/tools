
## without any ci, with local Docker/(VBox+Vagrant), with cloud issues tracker, gitflow repo, distrib repos

#- make distrib repos, if now exist
#- deploy local Docker/(VBox+Vagrant)
#- sign in/up on github/bitbucket
#- new empty repo on github/bitbucket, with issues tracker
#- make gitflow repo with tools
#- install IDE, like NetBeans, integrate it with gitflow repo
#- add source project to repo
#- activate IDE support Docker/(VBox+Vagrant) commands, images, services
#- set up buils/packages make and integrate it with distrib repos

#- git push and check release build on github/bitbucket
